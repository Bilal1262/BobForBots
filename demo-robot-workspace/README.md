# Demo colcon workspace for BobForBot

Four reference packages with intentional defects for MCP and safety-review demos.

Build (after sourcing ROS 2):

```bash
cd demo-robot-workspace
colcon build --symlink-install
```
